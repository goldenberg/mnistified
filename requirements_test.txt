mock==2.0.0
pytest==3.0.2
pytest-cov==2.3.1
pytest-flask==0.10.0   Never got to work correctly, so wrote my own fixtures in conftest.py with the same names for now.
scikit-image==0.12.3
tox==2.3.1
WebTest>=1.3.1
